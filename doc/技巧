android xml文件中：
        <b></b> 加粗
        <i></i> 斜体
        <u></u> 下划线

#布局CoordinatorLayout

# 提升编译速度：在项目gradle.properties 文件下添加以下代码即可
    android.enableBuildCache=true

# Android 布局优化
    1. include      解决重复布局
    2. ViewStub     延时加载(或者登录、未登录状态下显示不同布局)
    3. merge        减少布局嵌套冗余层级